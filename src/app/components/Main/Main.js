import React, { Fragment } from "react";
import NbaPlayers from "../react-spring-examples/NbaPlayers";

const Header = () => {
  return (
    <Fragment>
      <NbaPlayers />
    </Fragment>
  );
};

export default Header;
